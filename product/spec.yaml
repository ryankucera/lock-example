# Murano Product Spec File - Lock Example


resources:
-   alias: lock-command 
    format: string
    settable: true
-   alias: lock-state
    format: string
-   alias: battery-percent
    format: integer
