# Murano Product Spec File - Lock Example


resources:
-   alias: lock-command 
    format: string
    settable: TRUE
-   alias: lock-state
    format: string
-   alias: battery-percent
    format: integer
    settable: FALSE
